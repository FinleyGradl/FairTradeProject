# Database
# Docker Compose (default — run: docker compose up)
DATABASE_URL="postgresql://fairtrade:fairtrade@localhost:5432/fairtrade"

# Local SQLite fallback (no Docker needed):
# DATABASE_URL="file:./dev.db"

# Auth (Phase 3+)
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Maps (optional — prototype uses OpenStreetMap/Leaflet, no token required)
NEXT_PUBLIC_MAPBOX_TOKEN=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
